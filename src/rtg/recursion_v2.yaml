# src/rtg/recursion_v2.yaml
# ---------------------------------------------------------
# Parameters after 6 × (2×2×2) block-spin steps
# a_eff = a0 / 64  = 0.0808 fm
# ---------------------------------------------------------

recursion_depth : 6         # number of doublings
K_over_J        : 0.27      # K / J ratio AFTER recursion
J_ex_over_J     : 0.25      # (optional) exchange / main coupling
sigma_scale     : 0.5       # width shrink per step, sigma' = sigma / 2
